services:
  # Nginx リバースプロキシ
  nginx:
    image: nginx:1.25-alpine
    ports:
      - "8800:80"
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./backend:/var/www/html
      - ./frontend/out:/var/www/static
    depends_on:
      - php
      - frontend

  # PHP-FPM (Laravel)
  php:
    build:
      context: ./docker/php
    volumes:
      - ./backend:/var/www/html
      - ./docker/php/php.ini:/usr/local/etc/php/conf.d/custom.ini
    environment:
      - APP_ENV=local
    depends_on:
      - db
      - mailhog

  # Next.js 14 フロントエンド
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    ports:
      - "3800:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8800/api
    depends_on:
      - php

  # MySQL 8.0
  db:
    image: mysql:8.0
    ports:
      - "3380:3306"
    environment:
      MYSQL_DATABASE: tone_db
      MYSQL_USER: tone_user
      MYSQL_PASSWORD: tone_password
      MYSQL_ROOT_PASSWORD: root_password
    volumes:
      - db_data:/var/lib/mysql
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci

  # Mailpit（開発用メールサーバー・ARM64対応）
  mailhog:
    image: axllent/mailpit
    ports:
      - "1025:1025"   # SMTP
      - "8825:8025"   # Web UI

volumes:
  db_data:
